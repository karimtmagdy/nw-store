{
  "rewrites": [{ "source": "/(.*)", "destination": "/(.*)" }],
  "routes": [


    { "src": "/admin/(.*)", "dest": "/admmin/$1" },
    { "src": "/(.*)", "dest": "/(.*)" },

    { "handle": "filesystem" },

    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
