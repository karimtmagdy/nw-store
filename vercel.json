{
  "rewrites": [{ "source": "/(.*)", "destination": "/(.*)" }],
  "routes": [


  
    { "src": "/src/(.*)", "dest": "/src/(.*)" },

    { "handle": "filesystem" },

    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
